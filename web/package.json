{
  "name": "vibes-web",
  "private": true,
  "description": "Wrapper del frontend para ejecutar desde /web usando la app Next en la raíz",
  "scripts": {
    "dev": "cd .. && npm run dev",
    "build": "cd .. && npm run build",
    "start": "cd .. && npm run start",
    "lint": "cd .. && npm run lint",
    "test": "cd .. && npm test"
  }
}
